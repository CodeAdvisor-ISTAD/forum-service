spring:
  application:
    name: forum-code-advisors
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: qwerqwer
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true